<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrar Pagamento - MeuPet</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>REGISTRO DE PAGAMENTOS</h1>
    <form action="/pagamentos/registrar" method="POST">
        <label for="valor">Valor:</label>
        <input type="number" id="valor" name="valor" required>
        
        <label for="data">Data:</label>
        <input type="date" id="data" name="data" required>
        
        <label for="animal">ID do Animal:</label>
        <select id="animal" name="animal_id" required>
            {{#each animais}}
            <option value="{{this.id}}">{{this.nome}} (ID: {{this.id}})</option>
            {{/each}}
        </select>
        
        <label for="servico">Serviço:</label>
        <select id="servico" name="servico_id" required>
            {{#each servicos}}
            <option value="{{this.id}}">{{this.nome}}</option>
            {{/each}}
        </select>
        
        <button type="submit">Registrar</button>
    </form>
    <a href="/pagamentos/listar">Listar Pagamentos</a>
    <p>
        <a href="/">Voltar para a Página Inicial</a>
    </p>
</body>
</html>
